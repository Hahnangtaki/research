apply plugin: "war"

bootWar {
    mainClassName = "id.co.research.relation.RelationInOneTableApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
